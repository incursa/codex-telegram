#!/bin/sh
set -eu

if [ "$1" = remove ] && command -v systemctl >/dev/null 2>&1; then
    deb-systemd-invoke stop codex-telegram-updater.path >/dev/null 2>&1 || true
    deb-systemd-invoke stop codex-telegram.service >/dev/null 2>&1 || true
fi

exit 0
